# DirectXGame エンジン学習ノート（2026-07-03〜）

このドキュメントは、コードがブラックボックス化してしまった状態から「なぜこう動くのか」を初歩から順序立てて学び直すための学習メモです。対話形式で1トピックずつ進め、学んだ内容をここに追記していきます。

## 学習の進め方
- 対話形式で1トピックずつ「説明 → 理解確認 → 次へ」
- main.cpp から実際の起動順序通りに追う
- 「なぜこの設計か」を中心に学ぶ（コードは要点のみ引用、ファイルパス＋行番号を明記）

## 学習トピック一覧（進行順）

- [x] Topic 0: 全体マップ（main.cppから見る起動の一本道）
- [x] Topic 1: DirectXDevice — デバイス生成
- [x] Topic 2: CommandManager — コマンドキュー/アロケータ/リスト/フェンス
- [x] Topic 3: SwapChainManager + DescriptorHeaps
- [x] Topic 4: DirectXManager — まとめ役とフレームサイクル
- [x] Topic 5: Pipeline / PSO — DirectX12最大の壁
- [x] Topic 6: IDrawable実装（Cubeを代表例に）
- [x] Topic 7: Renderer — WVP計算とDraw呼び出しの束ね役
- [x] Topic 8: シェーダー（HLSL）とC++側の対応関係
- [x] Topic 9: Camera / ImGui統合
- [x] Topic 10: GameObject/Component層（移行中の二重構造）
- [x] Topic 11: Game.cpp — アプリケーション層の全体像
- [x] Topic 12: 軽量化の仕組み／ライティング×ブレンドモードの仕組み（追加トピック）
- [x] Topic 13: 外部ツール — ImGuizmoとAssimp（ギズモとアニメーション）（追加トピック）
- [x] Topic 14: その他のサブシステム — Texture/ShaderCompiler/Audio/Debug（追加トピック）
- [x] 付録: Gameから呼び出せる関数リファレンス

---

## Topic 0: 全体マップ — main.cpp から見る起動の一本道

### 結論：このエンジンは「2つの箱」でできている

```cpp
Engine engine;   // 土台（DirectX初期化・ウィンドウ・カメラ・ImGui・入力・音声）
Game   game;     // 中身（シーン構築・描画呼び出し・UI）

engine.Initialize(...);
game.Initialize(engine.GetRenderer(), engine.GetCamera());

while (engine.Update()) {      // ①
    game.Update(engine.GetDeltaTime());  // ②
    game.Render();                       // ③
    engine.Flush();                      // ④
}

engine.Finalize();
```

`main.cpp:8-24` にこれだけが書かれている。全体像を1枚の絵にすると：

```
┌─────────────── Engine（エンジン層。Gameを一切知らない） ───────────────┐
│  Window / DirectXManager / Renderer / Camera / ImGui / Input / Audio │
└────────────────────────────────────────────────────────────────────┘
                 ↑ GetRenderer() / GetCamera() で「貸し出す」だけ
┌─────────────── Game（アプリケーション層。Engineに依存する） ───────────┐
│  Cube/Sphere/Triangle等の配置・ImGuiパネル・ギズモ操作・ピッキング     │
└────────────────────────────────────────────────────────────────────┘
```

### なぜこう分けるのか

「**エンジン**」と「**そのエンジンで作る具体的なゲーム**」を意図的に分離する設計。

- `Engine` は「DirectX12を動かすための土台」を提供するだけで、Cubeが何個あるかも、Race the Sunがどんなゲームかも一切知らない。
- `Game` は `Engine::GetRenderer()` / `GetCamera()` で「描画する手段」と「カメラ」だけを借りて、それを使って好きなシーンを組み立てる。

**理由**：もしEngineの中に「Cubeを描画する」のようなゲーム固有のロジックが混じると、別のゲーム（Race the Sun本体）を作るときにEngineごと書き換える羽目になる。境界を「Renderer/Cameraを貸すだけ」に絞ることで、Engine側は今後どんなゲームを作っても再利用できる形になっている。ディレクトリ構成もそのまま `Engine/` と `Game/` に分かれているのはこのため。

### `while (engine.Update())` の中身（フレームループ）

| 呼び出し | 実際にやっていること |
|---|---|
| ① `engine.Update()` | ウィンドウメッセージ処理（閉じるボタン等）→ `BeginFrame`（GPUにこれから描画すると伝える）→ ImGuiフレーム開始。ウィンドウが閉じられたら`false`を返してループが終わる |
| ② `game.Update(deltaTime)` | シーン内オブジェクトの状態更新（今はデモなので回転アニメ等） |
| ③ `game.Render()` | Cube/Sphere/...を`Renderer`経由で「描画命令として積む」（まだGPUには送られていない） |
| ④ `engine.Flush()` | 積まれた描画命令をまとめて`FlushTriangles`等で実行 → ImGui描画 → `EndFrame`（GPUに実行させてPresent＝画面に出す） |

重要なのは、③の`game.Render()`は**即座に絵を描いているわけではない**という点。「これを描け」という命令をバッファに積んでいるだけで、実際にGPUに送られて画面に出るのは④の`Flush()`のタイミング。これはDirectX12（というよりモダンなGPU API全般）の基本的な考え方で、Topic 1〜4で「なぜCPU側で命令を貯めてからまとめてGPUに送るのか」を掘り下げる。

### 参照ファイル
- `main.cpp`
- `Engine/Engine.h`

---

## Topic 1: DirectXDevice — デバイス生成

### 4段階の処理（`DirectXDevice.cpp:7-12`）

```cpp
void DirectXDevice::Initialize() {
    InitializeCOM();   // ①COMを使う宣言
    CreateFactory();   // ②GPUを探す入り口(IDXGIFactory)を得る
    SelectAdapter();   // ③高性能GPUを優先列挙、ソフトウェアアダプタ除外
    CreateDevice();    // ④対応最高FeatureLevelでID3D12Deviceを取得
}
```

### 各段階の「なぜ」

- **①InitializeCOM**: DirectX/DXGIはWindowsのCOMの上に乗っている。使う前に`CoInitializeEx(MULTITHREADED)`で宣言が必要。
- **②CreateFactory**: DirectX12はデバイスを直接列挙できず、必ず`IDXGIFactory`経由でアダプタ(GPU)を探す。SwapChain生成でも再利用する。
- **③SelectAdapter**: 1台のPCには実GPUに加えてWindows製のソフトウェアレンダラ(WARP)もアダプタとして見える。`DXGI_ADAPTER_FLAG3_SOFTWARE`で除外し、`DXGI_GPU_PREFERENCE_HIGH_PERFORMANCE`で内蔵/外付けがある場合に高性能側を優先させる。
- **④CreateDevice**: FeatureLevelは「GPU/ドライバがDX12のどの機能セットまで対応するか」の段階値。12.2で決め打ちすると古いGPUで起動不能になるため、12.2→12.1→12.0の順に試して「対応可能な最高レベル」で妥協点を探すフォールバック設計。

### まとめ
```
CoInitializeEx → CreateDXGIFactory → EnumAdapterByGpuPreference(高性能優先/SW除外) → D3D12CreateDevice(12.2→12.1→12.0)
```
ここで得た `ID3D12Device device_` が、以降の全DirectX12リソース（CommandQueue・SwapChain・DescriptorHeap・バッファ...）の生成元になる。DirectXDeviceを一番最初にやる理由はこれ。

### 参照ファイル
- `Engine/Graphics/DirectXDevice/DirectXDevice.h`
- `Engine/Graphics/DirectXDevice/DirectXDevice.cpp`

---

## Topic 2: CommandManager — コマンドキュー/アロケータ/リスト/フェンス

### 4つのオブジェクト（`CommandManager.cpp:5-11`）

| クラス | 役割 | たとえ |
|---|---|---|
| `ID3D12CommandQueue` | GPUに命令列を投入する窓口 | 郵便ポスト |
| `ID3D12CommandAllocator` | 命令列を書き込むメモリの実体 | 便箋の束 |
| `ID3D12GraphicsCommandList` | CPU側が命令を記録していく操作口 | ペン先／書いている内容 |
| `ID3D12Fence` | GPUの実行完了をCPUに知らせる同期の目印 | 配達完了通知 |

### なぜFenceが必要か
CPU/GPUは非同期。Fenceが無いと、GPUがまだ前フレームを描画中なのにCPUが次フレーム用にバッファを上書きしてしまい、表示崩壊やクラッシュにつながる。
```cpp
Signal(): fenceValue_++; commandQueue_->Signal(fence_, fenceValue_);       // 「ここまで来たらこの値に」と予約
WaitForFence(): if (fence_->GetCompletedValue() < fenceValue_) { GPU完了までCPUをブロック }
```

### CommandListのライフサイクル
```
Reset(記録開始) → 描画命令を積む → Close(記録終了)
  → ExecuteCommandLists(GPU投入) → Signal(目印) → Present
  → WaitForFence(完了待ち) → Reset(次フレーム再利用)
```
`ResetCommandList()`の直前に`WaitForFence()`が必要な理由: GPUがまだ読んでいるかもしれないメモリを、完了を待たずにリセットしてはいけないため。

Topic0の「Render()は積むだけ、Flush()で実行」の実体はこれ。Render中は「命令を積む」段階、Flush中の`EndFrame`で`Close→Execute→Signal→Present→Wait→Reset`が走る。

### 参照ファイル
- `Engine/Graphics/CommandManager/CommandManager.h`
- `Engine/Graphics/CommandManager/CommandManager.cpp`

---

## Topic 3: SwapChainManager + DescriptorHeaps

### なぜバッファが2枚（ダブルバッファリング）か
`swapChainDesc.BufferCount = 2`。1枚だと「表示中の絵」に「次のフレーム」を上書きしてしまいちらつく。2枚用意し「表示中」「裏で描画中」を役割分担、`Present()`で入れ替える（`DXGI_SWAP_EFFECT_FLIP_DISCARD`）。

### なぜリソースバリア（PRESENT⇔RENDER_TARGET）が必要か
バックバッファは「表示用」と「描画先」で異なる用途(GPU内部の扱い)を持つため、切り替え時に「これから用途が変わる」とGPUに明示する必要がある。伝えずに使うと未定義動作。
```
PRESENT → [バリア] → RENDER_TARGET → 描画 → [バリア] → PRESENT → Present()
```

### RTV/DSV/SRVの違い
リソース本体と「どう解釈して使うか(View)」が分離されている。

| 種類 | 意味 | 用途 |
|---|---|---|
| RTV | ここに色を描き込む | バックバッファ2枚分 |
| DSV | ここに深度を書く | 深度バッファ1枚 |
| SRV | シェーダーから読み取り専用参照 | テクスチャ・WVP等のStructuredBuffer |

### なぜSRVヒープだけ1024個も確保され特別か
SRVはテクスチャ1枚・オブジェクトのWVPバッファごとに必要で数が多い。またSRVヒープだけ`shaderVisible=true`（シェーダーから直接参照可能）。DX12はシェーダーが見えるヒープ数に制限があるため、テクスチャもWVPも全部同じ1つのSRVヒープにまとめ、`BeginFrame`で1回だけバインドする設計（Topic4）。

### 参照ファイル
- `Engine/Graphics/SwapChainManager/SwapChainManager.h/.cpp`
- `Engine/Graphics/DescriptorHeaps/DescriptorHeaps.h`

---

## Topic 4: DirectXManager — まとめ役とフレームサイクル

### 何をするクラスか
Topic1〜3の3クラス(`DirectXDevice`/`CommandManager`/`SwapChainManager`)をメンバとして所有し、正しい順序で初期化・呼び出すだけの「指揮者」。新しい処理は何もしていない。
```cpp
device_.Initialize();                                    // Topic1
command_.Initialize(device_.GetDevice());                // Topic2（deviceが要る）
swapChain_.Initialize(factory, device, commandQueue,...); // Topic3（3つとも要る）
```
この順序は依存関係グラフ（後のクラスほど前のクラスの出力を引数に取る）をそのままコードに落とし込んだもの。

### BeginFrame/EndFrameの中身（Topic1〜3の集大成）
```
BeginFrame: PRESENT→RENDER_TARGETバリア → RTV設定+クリア → Viewport設定 → SRVヒープを1回だけバインド
EndFrame:   RENDER_TARGET→PRESENTバリア → Close → ExecuteCommandList → Signal → Present → WaitForFence → ResetCommandList
```

### なぜSRVヒープのバインドはBeginFrameで1回だけか
DX12はシェーダーから見えるヒープを同時に1つしかバインドできない制約がある。テクスチャもWVPも色も全部同じSRVヒープにまとめてあるからこそ、フレーム最初に1回`SetDescriptorHeaps`すれば済み、以降は各オブジェクトが「ヒープの何番目か(インデックス)」を指定するだけでよい。ヒープを切り替える操作はGPU側で重いため、これを避ける設計。

### 参照ファイル
- `Engine/Graphics/Renderer/DirectXManager.h/.cpp`

---

## Topic 5: Pipeline / PSO — DirectX12最大の壁

### PSOとは・なぜ壁か
PSO(Pipeline State Object)は描画パイプライン全ステージ（VS/PS/ブレンド/深度/ラスタライザ等）の設定をまとめた**不変オブジェクト**。DX11と違い、作った後に設定を1つでも変えることができず、変えるには別PSOを新規作成するしかない。DX12がAPIオーバーヘッドを減らすため「実行時に何が変わるか分からない」状態をドライバから排除した設計思想のため。

### 解決策：組み合わせぶんPSOを事前生成
```cpp
ComPtr<ID3D12PipelineState> graphicsPipelineStates_[BlendMode::kCount][2]; // 6種×αTest有無2 = 12個
```
`CreatePSO()`が起動時に12個全部生成し、実行時は`GetPipelineState(blendMode, alphaTest)`で選んで`SetPipelineState()`するだけ。「事前に全部作って実行時は選ぶだけ」がPSOの制約への回答。

### なぜαTestを別バリアントに分けるか（Early-Z最適化）
シェーダーに`discard`命令が**存在するだけで**多くのGPUドライバはEarly-Z（深度テストでの早期棄却）を無効化する。αTest不要な大多数のオブジェクト（グリッド等）まで一律discard入りPSを使うと最適化を失うため、discard有無で別コンパイルして使い分ける。

### BlendModeごとの合成式
| Mode | 式 | 用途 |
|---|---|---|
| kNone | Src(上書き) | 通常不透明 |
| kNormal | Src*s+Dest*(1-s) | 半透明 |
| kAdd | Src*s+Dest | 光・炎 |
| kSubtract | Dest-Src*s | 暗くする |
| kMultiply | Dest*SrcColor | 影・汚れ（強さ調整不可） |
| kScreen | Src+Dest*(1-SrcColor) | 発光（強さ調整不可） |

`s`はCPUから`OMSetBlendFactor()`で渡す定数。kMultiply/kScreenは式にSrcColor(ピクセル毎の値)が入るため定数1つでは強さを補間できない。

### なぜ半透明は深度を書き込まないか
深度を書き込むと、色は薄く合成されて見えるのに奥のオブジェクトが深度テストで棄却され描かれなくなる（半透明の奥が消えるバグ）。そのため半透明系は「深度比較はする、書き込みはしない」設定。

### 参照ファイル
- `Engine/Graphics/Pipeline/PipelineBase.h/.cpp`
- `Engine/Graphics/Pipeline/BlendMode.h`

---

## Topic 6: IDrawable実装（Cubeを代表例に）

### IDrawableは最小限の共通契約
```cpp
class IDrawable {
    virtual void Draw(commandList, instanceCount, startInstance=0) = 0;
    virtual ID3D12RootSignature* GetRootSignature() const = 0;
    virtual ID3D12PipelineState* GetPipelineState() const = 0;
};
```
Cube/Sphere/Triangle/Sprite/Modelが継承するが、実装はほぼ独立。継承は薄い。

### Cubeが持つ4つのもの
1. **頂点バッファ**（`vertexResource_`）: 形そのもの。36頂点(6面×2三角形×3頂点)ぶんしか持たない。100個描いても複製しない
2. **InstancedWvpColorBuffer**（`wvpColorBuffer_`）: 個体ごとのWVP行列・色をStructuredBufferの配列で持つ。`SetWvpMatrix(wvp, world, index)`でindex番目の個体を更新。**継承ではなくコンポジション**で持たせている（「各Objectクラスは互いに無関係のIDrawable実装であり、共通の親を作ると関係性が歪む」ため。振る舞いだけ共通なので継承でなくヘルパー共有）
3. **rootSignature_ / pipeline_**: 自分では作らずTopic5のPipelineから借りるだけ。同じ形式で描くもの同士（Cube/Sphere/Sprite/非スキニングModel）は同じPipelineインスタンスを共有

### SetPipelineCommands → Draw の流れ
```cpp
SetPipelineCommands(...): pipeline_->GetPipelineState(blendMode, alphaTest)でPSO確定
                            → SetPipelineState + テクスチャ/WVP/色のSRVをバインド
Draw(instanceCount): IASetVertexBuffers → IASetPrimitiveTopology(TRIANGLELIST)
                      → DrawInstanced(36頂点, N個, ...)  ← 1回でN個まとめて描画
```
`DrawInstanced`でまとめてN個描くのが、CPU側のDrawCall数を減らす仕組み（インスタンシング）。GPU側は`SV_InstanceID`で自分が何番目かを知り、その番号でWVP/色配列を引く（Topic8）。

### 参照ファイル
- `Engine/Graphics/Object/IDrawable.h`
- `Engine/Graphics/Object/Cube/Cube.h/.cpp`
- `Engine/Graphics/ResourceFactory/InstancedWvpColorBuffer.h`

---

## Topic 7: Renderer — WVP計算とDraw呼び出しの束ね役

### 二層の「積む→まとめて描く」構造
`Game::Render()`内で`renderer_->DrawCube(...)`を何度呼んでも、その場ではGPUに何も積まれない。`cubeCommands_`(std::vector)に「意図」が溜まるだけ。`Engine::Flush()`内の`FlushCubes()`で初めて実際にDrawされる。Topic0の「Render/Flushの分離」がRenderer内部にもう1段ある。

### WVP計算は全DrawXxxで同一パターン
```cpp
Matrix4x4 world = TransformMath::MakeAffineMatrix(t.scale, t.rotation, t.translation);
Matrix4x4 wvp   = world * view_ * projection_;
```
2Dスプライトだけ`view_*projection_`でなく`MakeOrthographicMatrix()`（正射影、カメラを介さないUI用）。

### FlushBatch：なぜソートしてからバッチ化するか
PSO切り替え(SetPipelineState)はそこそこ重い操作。texture→blendMode→blendStrength→alphaTest等でソートし、同じ設定が連続する区間ごとに`SetPipelineCommands`を1回だけ呼び、その区間ぶんまとめて`DrawInstanced`する。ソート後の新しい並び順のindexでStructuredBufferに書き込む必要があるため、ソート確定後に全件`SetWvpMatrix/SetColor`してから区間分割する。

### FlushBatchがテンプレートな理由
Triangle/Cube/Sphereは形は違うが「SetWvpMatrix/SetColor/SetPipelineCommands/Drawという同一シグネチャを持つ」点だけ共通。継承ではなくテンプレートで共通化（Topic6のIDrawableの薄い継承と一貫した考え方）。

### Model/Sprite3D/Sprite2Dはなぜ別ルート(IssueDrawCommand)か
Modelはモデルごとに頂点データ自体が違い「形は共通、個体差だけ配列」という前提が成立しないためバッチ化せず1件ずつ処理。Sprite類も画面内の枚数が少なくバッチングの価値が薄いため同様。

### 参照ファイル
- `Engine/Graphics/Renderer/Renderer.h/.cpp`

---

## Topic 8: シェーダー（HLSL）とC++側の対応関係

### SV_InstanceIDの罠とgInstanceOffset
`DrawInstanced`の`StartInstanceLocation`を指定しても`SV_InstanceID`は常に0始まり（オフセットされない）。バッチの2番目以降の区間を正しくWVP配列で引くため、CPU側が`SetGraphicsRoot32BitConstant`で開始位置を`b1`(`gInstanceOffset`)として渡し、VS側で`instanceId + gInstanceOffset`と手動で足し戻す。

### レジスタ対応表
| レジスタ | 意味 | C++側 |
|---|---|---|
| t0 | テクスチャ | textureManager→GetSrvGpuHandle |
| t1 | WVP/World/WorldInverseTranspose配列 | wvpColorBuffer_.GetWvpSrvHandle |
| t2 | 色配列 | wvpColorBuffer_.GetColorSrvHandle |
| b0 | LightData | light_.GetGPUAddress |
| b1 | InstanceOffset | SetGraphicsRoot32BitConstant(start) |
| b2 | BlendData(mode/strength/alphaTest) | PipelineCommandHelper::ApplyCommon |

RootSignature＝関数シグネチャの契約、各Draw前のSetGraphicsRoot*＝実引数、という対応。

### PSが全オブジェクト共通な理由
`Object3d.PS.hlsl`はCube/Sphere/Triangle/Sprite/非スキニングModelで共用。オブジェクト種別でシェーダーを分けず、`gEnableLighting`等のフラグ組み合わせ（データ駆動）で挙動を変える設計。[[project_race_the_sun]]の「ライティングは既にフラグ/データ駆動でコンポーネント的」の実体。

### #ifdef ENABLE_ALPHA_TEST（Topic5の実物）
`PipelineBase::PixelShader(enableAlphaTest)`が渡すdefineでコンパイル時に分岐、discard命令の有無で別バリアントを生成（Early-Z温存のため）。しきい値ちょうど1.0での誤discardを防ぐため`1e-5f`の微小値を加算。

### kMultiply/kScreenの強さ調整はPS側でlerp
`OMSetBlendFactor`の定数だけでは強さを補間できないため、PS側で出力色を「効果なしの色」（乗算なら白、スクリーンなら黒＝各ブレンド式の単位元）へ`lerp`することで疑似的に強さを再現。

### 参照ファイル
- `HLSL/Object3d.hlsli`, `Object3d.VS.hlsl`, `Object3d.PS.hlsl`

---

## Topic 9: Camera / ImGui統合

### View行列はなぜ逆行列か
カメラの位置・回転から作るアフィン行列はCubeのWorld行列と同じ計算だが、View行列にするにはその**逆行列**を使う。「カメラが右に動く」＝画面上は「世界が左に動く」のと同じなので、カメラを原点・無回転に据えたときの他オブジェクトの見え方＝カメラ自身のワールド行列の逆行列、という理屈。

### Camera入力
右クリック中のみマウスでrotation変更（回転はX軸±1.55rad=約88度でクランプ、ジンバルロック的違和感を防止）。WASD移動は`localMove`をカメラの回転行列で変換してからワールド座標に加算＝「カメラが向いている方向」に進む。

### ImGuiのBeginFrame/EndFrameもフレーム境界パターンを踏襲
```cpp
Engine::Update(): directX_.BeginFrame() → ... → imgui_.BeginFrame()
Engine::Flush():  各Flush*() → FlushSprites2D()(最後) → imgui_.EndFrame() → directX_.EndFrame()
```
同じコマンドリストに積む以上、後から積んだものが上に重なる。3D→2Dスプライト(Depth無効で常に最前面)→ImGuiの順で「ゲーム画面の上にUIが乗る」自然な見た目になる。

### Finalizeの順序にも理由がある
`directX_.WaitForGPUCompletion()`を終了処理の最初に呼ぶ。GPUが直前フレームのコマンドを実行完了する前にImGui/RendererがGPUリソース(フォントテクスチャ等)を解放してしまわないため（Topic2のFence待機の考え方が終了処理にも適用されている）。

### Initialize全体の依存順序（総まとめ）
`Debug::EnableDebugLayer()`はデバイス作成前でないと効果がない→`directX_.Initialize()`より前。`WaitForGPUCompletion()`が`renderer_.Initialize()`直後にあるのは、初期化中(テクスチャアップロード等)に積んだコマンドをフレームループ前に実行完了させるため。

### 参照ファイル
- `Engine/Camera/Camera.h/.cpp`
- `Externals/imgui/imguiManager.h/.cpp`
- `Engine/Engine.cpp`

---

## Topic 10: GameObject/Component層（移行中の二重構造）

### 訂正：Componentパターンは既に導入済み
以前のメモリでは「Componentパターン導入はフェーズ1まで保留」としていたが、実際は`Game.cpp`全体でCube/Sphere/Triangle/Floor/Sprite3D/Sprite2D/Model/FBXModelすべてが`GameObject`+`XxxRenderComponent`で構築済み（メモリ側を後で更新）。

### IComponentは最小限（Update()のみ）
描画は`Game::Render()`→`Renderer`という既存フロー（Topic7）と衝突しないよう、IComponent自体はRender相当のメソッドを持たない。描画コンポーネントは`Draw(Renderer*, Transform)`という独自メソッドを個別に持つ。

### GameObjectは「Transform + 任意機能の入れ物」、Rendererを知らない
```cpp
class GameObject {
    Transform transform;
    template<T> T* AddComponent(...);
    template<T> T* GetComponent() { dynamic_cast探索 }
};
```
Rendererを直接呼ぶのはComponent側。`GetComponent<T>()`が`dynamic_cast`なのは、1つのGameObjectが複数種類のComponent（例: CubeRenderComponent+OBBColliderComponent）を持てるため型ごとに検索する必要があるから。

### 実例：1つのCubeが「描画」と「当たり判定」を組み合わせる
`cubeObject_`は`CubeRenderComponent`+`OBBColliderComponent`を両方持つ。回転はコライダー自身が持たずtransform.rotationを共有参照するため、Cube回転で当たり判定も自動追従する。これがComponentパターン本来の「機能の組み合わせ」の実例。

### RenderComponentBaseはTopic7のDrawXxx引数パターンをそのまま写した薄いラッパー
`CubeRenderComponent::Draw()`の中身は`renderer->DrawCube(...)`を呼ぶだけ。低レベルObject層（Topic6-7）を置き換えるのではなく、Transform/Collider等と一体管理するための上位整理レイヤー。

### なぜグリッド(2500個)だけGameObjectを介さないか
`gridCubes_`は`std::vector<Transform>`のまま、直接`renderer_->DrawCube()`を呼ぶ。2500個が完全に同一の性質を持つ大量静止インスタンスのため、GameObjectのオーバーヘッド(unique_ptr間接参照・dynamic_cast型検索)を避ける意図的な最短経路。

### 参照ファイル
- `Engine/GameObject/GameObject.h`, `IComponent.h`
- `Engine/GameObject/Component/RenderComponentBase.h`, `CubeRenderComponent.h/.cpp`
- `Game/Game.cpp`（58-148行目のInitialize、196-221行目のRender）

---

## Topic 11: Game.cpp — アプリケーション層の全体像

### 公開インターフェースは3つだけ
`Initialize/Update/Render`のみ。815行の内訳はほぼ全部privateメンバ・ヘルパー（デモシーン構築・毎フレーム更新・約400行のImGuiデバッグUI）。性質の異なる3つの仕事が同居しているのがファイル肥大化の理由（[[project_engine_issues]]の既知負債）。

### Update は今はカメラ操作+FPS計測のみ
Race the Sunフェーズでは機体前進・障害物移動・当たり判定チェック等がここに追加されていく。

### Render はTopic6-10の集合場所
View/Projection計算(Topic9)→SetCamera(Topic7)→GameObject経由描画(Topic10)→グリッドは直接DrawCube(Topic10の例外ルート)→ピッキング/ギズモ更新→残りのGameObject描画→Collider可視化、という流れ。

### フラストラムカリングはCPU側の最適化（GPU側のバッチング/PSOとは別レイヤー）
カメラの視界外のグリッドCubeは`DrawCube`自体を呼ばずスキップ。呼ぶこと自体（WVP計算・StructuredBuffer書き込み・コマンド蓄積）のCPUコストを避けるのが目的。Topic7のバッチング（GPU側のSetPipelineState削減）とは別の最適化。

### エンジン一般論 vs デモ固有の境界
Topic1-9(+10の枠組み)はRace the Sunでもそのまま使うエンジン部分。Game.cppの具体的な中身（Cube/Sphere配置、グリッド負荷テスト、ギズモ、400行のImGuiパネル）は今後Player/Obstacle/SunSystem等に置き換わっていくデモ実験用の部分。

### 参照ファイル
- `Game/Game.h`
- `Game/Game.cpp`

---

## Topic 12: 軽量化の仕組み／ライティング×ブレンドモードの仕組み

### 軽量化：実装済み最適化まとめ
1. インスタンシング（頂点1個共有+StructuredBuffer）Topic6
2. バッチング（ソート+まとめてDraw）Topic7
3. フラストラムカリング（画面外はDrawCube自体を呼ばない）Topic11
4. αTest無効バリアント分離（Early-Z温存）Topic5/8
5. WorldInverseTransposeキャッシュ（回転が変化した時だけ再計算）Topic7
6. **SceneLightのON/OFF用バッファを2本用意**（新出）：`resource_`(ON,毎回Upload)と`resourceOff_`(`enableLighting=0`固定・二度と更新しない)を最初から作っておき、`GetGPUAddress(useLighting)`でどちらを使うか選ぶだけにする。値の書き換えより「事前に用意した状態を選ぶだけ」の方が軽い。PSO事前生成(Topic5)と同じ発想
7. SRVヒープ1本化+フレーム先頭で1回だけバインド Topic4

未解決の課題：グリッドの毎フレーム個別DrawCall、毎DrawCallでのSetPipelineState/RSSetViewports重複設定、Present+Fenceの二重同期（[[project_engine_issues]]）

### ライティング×ブレンドモード：2段階のブレンド
```
①PS内(プログラマブル): テクスチャ×頂点色 → αTest discard → ライティング(diffuse乗算+specular/rim加算) → BlendMode強さのlerp(kMultiply/kScreenのみ)
②GPU固定機能(PSOのBlendState、Topic5): ①で決まったSrc色 と 画面のDest色 を SrcBlend/DestBlend/BlendOpで合成
```
ライティングは①の内側（Src色を作る過程）でなければ意味がない＝②(画面合成後)には適用できない。

`useLighting`と`blendMode`はRenderer::DrawCommandBaseの別々のフィールドで独立した軸。4通りの組み合わせが可能（例: kAdd+lighting=trueは光を浴びて光る半透明、kAdd+lighting=falseは自己発光パーティクル）。

### 参照ファイル
- `Engine/Graphics/Lighting/SceneLight.h/.cpp`
- `HLSL/Object3d.PS.hlsl`
- `Engine/Graphics/Renderer/Renderer.h`

---

## Topic 13: 外部ツール — ImGuizmoとAssimp（ギズモとアニメーション）

### ImGuizmo：3D操作UIを自作しない
`ImGuizmo::Manipulate(view, proj, operation, world行列ポインタ)`にView/Projection(Topic9)とWorld行列(Topic7)を渡すだけで、ドラッグ操作・ハンドル描画・レイキャスト判定を丸ごと引き受けてくれる。戻り値trueならworld行列が書き換わっているので`DecomposeMatrixToComponents`でTransformに分解し戻す。

**一時バッファ(scratch)パターン**: PointLight(回転/スケールなし)・SpotLight(方向≠回転)・Collider(offset/radius)はTransform型ではないため、Transform型の代理(`lightGizmoScratch_`等)を経由してImGuizmoに渡し、変更があればSetter経由で本来のデータへ書き戻す。ドラッグ中(`ImGuizmo::IsUsing()`)は代理への再変換をスキップする＝方向→回転の逆算がジンバルロック付近で不安定なため、毎フレーム再計算するとハンドルが暴れるのを防ぐ。

### Assimp：複雑なフォーマットの自作パーサーを避ける
OBJは単純なテキスト形式なので自前パーサー(`Model_ObjLoader.cpp`)で学習教材として妥当。FBX/gltf等はボーン階層・キーフレーム・マテリアル階層が複雑で自作コストが見合わないため`Assimp`（`aiScene`という統一中間表現）に任せる。

### アニメーション（GPUスキニング）の仕組み
- ボーン階層は木構造(`NodeData`, DFS順で親が子より前)。1パスの前方走査でワールド変換を計算できる
- `boneMatrix = boneOffsetMatrix(逆バインド行列) * globalTransform`：頂点をボーンのローカル空間に戻してから今のボーン変換を掛け直す
- キーフレーム補間：位置/スケールは線形補間、回転だけクォータニオンSLERP（線形補間だと回転がねじれるため）
- GPU側(`SkinnedObject3d.VS.hlsl`)：頂点ごとに最大4本のボーンindex+weightを持ち、重み付き合成(`skinMatrix`)してから通常のWVP変換に合流。関節付近で1本だけだと不自然な折れ目ができるための対策
- Topic5の`SkinnedPipeline`が別クラスな理由（頂点入力レイアウトが違う）、Topic8の`t3`レジスタ（ボーン行列パレット）がここに繋がる

### 参照ファイル
- `Externals/ImGuizmo/`
- `Game/Game.cpp`（UpdateGizmo 434-513行目）
- `Engine/Graphics/Object/Model/Model.h`, `Model_SkeletalAnimation.cpp`
- `HLSL/SkinnedObject3d.VS.hlsl`

---

## Topic 14: その他のサブシステム — Texture/ShaderCompiler/Audio/Debug

### TextureManager：2段階アップロード
GPU専用メモリ(DEFAULT)にはCPUから直接書けないため、CPUから書ける中間バッファ(intermediate)に置いてからGPU自身にコピーさせる(`UpdateSubresources`)→`COPY_DEST→GENERIC_READ`バリア。テクスチャハンドル＝SRVヒープのインデックスそのもの。handle=0は白テクスチャ(`kTextureNone`)で「テクスチャなし」を分岐せず統一的に扱う。`kMaxTextureCount=500`はTopic3のSRVヒープ先頭帯予約と対応。

### ShaderCompiler：DXCで実行時コンパイル
`.hlsl`を起動時にその場でバイトコード化。事前コンパイルでなく実行時コンパイルなのはシェーダー修正の即時反映と、`defines`によるバリアント生成(Topic5のENABLE_ALPHA_TEST)を柔軟に行うため。

### AudioManager：XAudio2の3階層
SourceVoice(個々の音)→SubmixVoice(BGM/SE別のバス)→MasteringVoice(最終出力)。バスを分けるのはグループ単位で音量制御したいため。シングルトンなのは音声デバイスがアプリ全体で1つだけでよいため。

### Debug：クラッシュダンプとGPUバリデーション
`RegisterCrashHandler`は未処理例外時に`.dmp`を自動保存。`EnableDebugLayer()`はデバイス作成より前でないと効果がない(Topic9のInitialize順序の裏付け)。InfoQueueの`denyIds`に`LIVE_OBJECT_SUMMARY`が入っているのは、終了時に必ず出る定型警告が`SetBreakOnSeverity(WARNING,true)`と組み合わさってデバッガ未アタッチ時に誤ってアプリを落としてしまうのを防ぐため（実際に遭遇した問題への対処が跡として残っている）。

### 参照ファイル
- `Engine/Graphics/Texture/TextureManager.h/.cpp`
- `Engine/Graphics/ShaderCompiler/ShaderCompiler.h`
- `Engine/Audio/AudioManager.h`
- `Debug/Debug.h/.cpp`

---

# 付録: Gameから呼び出せる関数リファレンス

`Game`クラス（や今後作るPlayer/Obstacle等）が実際に触ることになる公開APIを、カテゴリ別にまとめる。すべて`Game::Initialize(Renderer* renderer, Camera* camera)`で受け取った`renderer_`/`camera_`ポインタ経由（Topic0）。

## 1. Math型（`Math/MathTypes.h`）

```cpp
using Vector2 = DirectX::XMFLOAT2;
using Vector3 = DirectX::XMFLOAT3;
using Vector4 = DirectX::XMFLOAT4;
using Matrix4x4 = DirectX::XMFLOAT4X4;

struct Transform {
    Vector3 scale       = {1,1,1};
    Vector3 rotation    = {0,0,0};  // ラジアン
    Vector3 translation = {0,0,0};
};
struct UVTransform {
    Vector2 offset = {0,0}; float rotation = 0.0f; Vector2 scale = {1,1};
};
```
オブジェクトの位置・向き・大きさは基本的に`Transform`1つで表現する（Topic7）。回転は度数ではなくラジアン。

## 2. GameObject / Component（Topic10）

```cpp
// ---- ①GameObjectを1つ用意する ----
GameObject enemyObject;
enemyObject.name = "Enemy";                 // ImGui/ギズモ表示名
enemyObject.transform.translation = {0,1,0};
enemyObject.pickingRadiusHint = 1.0f;       // マウスピッキング用の基準半径（Model系のみ通常上書きが必要）

// ---- ②見た目（RenderComponent）を1個だけ付ける。種類ごとに専用クラスがある ----
CubeRenderComponent* enemyRender = enemyObject.AddComponent<CubeRenderComponent>();
// 他の形が使いたければ、代わりに以下のどれか1つを付ける：
//   SphereRenderComponent*   ...AddComponent<SphereRenderComponent>();
//   TriangleRenderComponent* ...AddComponent<TriangleRenderComponent>();
//   SpriteRenderComponent*   ...AddComponent<SpriteRenderComponent>(/*is3D=*/true);
//   ModelRenderComponent*    ...AddComponent<ModelRenderComponent>(modelHandle, /*hasAnimation=*/false);

// RenderComponentBase共通プロパティ（Cube/Sphere/Triangle/Sprite/Model全部が持つ）
enemyRender->color         = {1,1,1,1};
enemyRender->lighting      = true;
enemyRender->textureHandle = someTextureHandle;
enemyRender->blendMode     = BlendMode::kNone;
enemyRender->blendStrength = 1.0f;
enemyRender->alphaTest     = false;
enemyRender->alphaThreshold = 0.5f;

// ---- ③当たり判定（ColliderComponent）を必要なら追加で付ける（同じenemyObjectに②と併設できる） ----
OBBColliderComponent* enemyCollider = enemyObject.AddComponent<OBBColliderComponent>();
enemyCollider->offset   = {0,0,0};
enemyCollider->halfSize = {1,1,1};  // 回転はenemyObject.transform.rotationをそのまま使う（コライダー自身は回転を持たない）
// 球で判定したいときは代わりに：
//   SphereColliderComponent* ...AddComponent<SphereColliderComponent>();
//   ->offset = {0,0,0}; ->radius = 1.0f;

// ---- ④毎フレームRender()内で、付けたコンポーネントを取り出して描画する ----
enemyObject.GetComponent<CubeRenderComponent>()->Draw(renderer_, enemyObject.transform);
// Modelだけ signature が違う（アニメーション更新のため deltaTime を追加で渡す）：
//   enemyObject.GetComponent<ModelRenderComponent>()->Draw(renderer_, enemyObject.transform, deltaTime_);
```

当たり判定の実際の交差チェックは`Collision`名前空間（下記4）を使う。`enemyCollider->GetWorldOBB(enemyObject.transform)`でローカルのoffset/halfSizeをワールド空間の`Collision::OBB`に変換してから、`Collision::OBBOBB(obbA, obbB)`のような関数に渡す。

### 補足：なぜ形ごとに専用のRenderComponentが要るのか

1. **Rendererに統合された1つの入り口が無い**：`DrawCube`/`DrawSphere`/`DrawModel`等は、Topic6-7で見た通りそれぞれ完全に別のGPUリソース（別の頂点バッファ/InstancedWvpColorBuffer/Rendererの別々のコマンドキュー）を持つ独立した`IDrawable`実装のため、そもそも1つの汎用関数にまとまっていない
2. **型ごとに必要なデータ/引数が違う**：`Sprite`は`UVTransform`、`Model`は`ModelHandle`+`hasAnimation`+`deltaTime`引数と、形ごとに固有の要素を持つ。1個の汎用クラスにすると使わないフィールドを全インスタンスが抱えることになる
3. **`RenderComponentBase`は「本当に共通な部分」（color/lighting/textureHandle/blendMode/blendStrength/alphaTest/alphaThreshold）だけを持つ**：Topic7で見た「DrawXxx各関数が共通して持つ末尾の引数パターン」をそのまま切り出したもの。共通部分は基底に、違う部分は派生に、という最小限の継承
4. **`IComponent::Draw`は仮想関数にしていない**（Topic10）：呼び出し側(`Game::Render()`)が`GetComponent<CubeRenderComponent>()`のように既に型を知った上で呼ぶため、実行時ポリモーフィズムは不要。型ごとに独立クラス+非仮想`Draw`にすることで、引数の組み合わせ違いがコンパイル時に検出される（enum+switch方式だと型と引数の対応が壊れてもコンパイルが通ってしまう）

## 3. Renderer — 描画API（Topic7）

**毎フレーム最初に1回**（Game::Render()の先頭で）：
```cpp
renderer_->SetCamera(view, proj, cameraPosition);  // 以降のDrawXxx(Transform)はこの行列を使う
```

**読み込み系**（Initialize時に1回）：
```cpp
TextureHandle tex = renderer_->LoadTexture("Resources/hoge.png");           // 同じパスは二重ロードしない
Renderer::ModelHandle model = renderer_->LoadModel("Resources/Model", "player.obj");
renderer_->UpdateModelAnimation(model, deltaTime); // ボーンアニメーションを進める（毎フレーム）
```

**描画系**（積むだけ、実行はEngine::Flush()、Topic7）：
```cpp
renderer_->DrawCube(transform, color, texture, useLighting, blendMode, blendStrength, alphaTest, alphaThreshold);
renderer_->DrawSphere(transform, color, texture, ...);      // 引数パターンは同じ
renderer_->DrawTriangle(transform, color, texture, ...);
renderer_->DrawModel(modelHandle, transform, color, texture, ...);
renderer_->DrawSprite3D(transform, color, texture, useLighting, uvTransform, blendMode, ...);  // ワールド空間スプライト
renderer_->DrawSprite2D(transform, color, texture, useLighting, uvTransform, blendMode, ...);  // 画面ピクセル座標UI
renderer_->DrawLine(start, end, color, view, projection);   // 単発の線
renderer_->DrawGridBatch(view, projection);                  // 事前登録した地面グリッド線をまとめて描画

// 回転が変化しない大量静止インスタンス向け（WorldInverseTranspose手渡しでInverse+Transpose省略、Topic7/11）
renderer_->DrawCube(transform, worldInverseTranspose, color, texture, ...);
```

**グローバル設定系**：
```cpp
renderer_->SetCubeSmoothness(0.5f);       // 0=フラット, 1=スムースシェーディング
renderer_->SetTriangleSmoothness(1.0f);
renderer_->SetSphereSubdivision(16);      // 分割数、上限は Renderer::kSphereMaxSubdivision
```

**ライティング**（`renderer_->GetLight()`でSceneLight&を取得、Topic12）：
```cpp
auto& light = renderer_->GetLight();
light.SetDirection({0,-1,0});      light.SetColor({1,1,1});       light.SetAmbient(0.2f);
light.SetEnableDirectional(true);  light.SetHalfLambertPower(2.0f); light.SetEnableToon(false);
light.SetCameraPosition(pos);      light.SetShininess(20.0f);     light.SetEnableSpecular(false);
light.SetEnableRim(false);         light.SetRimColor({1,1,1});     light.SetRimPower(2.0f);
light.SetEnablePointLight(false);  light.SetPointPosition(pos);    light.SetPointColor(c); light.SetPointIntensity(1.0f);
light.SetEnableSpotLight(false);   light.SetSpotPosition(pos);     light.SetSpotDirection(dir);
```
**必ずSetter経由で呼ぶこと**（Topic12/13）——`GetData()`で得た参照を直接書き換えると`Upload()`が呼ばれずGPUに反映されない。

**フレーム末**（`Engine::Flush()`が自動で呼ぶので通常Game側からは不要、参考として）：
```cpp
renderer_->FlushTriangles(); renderer_->FlushCubes(); renderer_->FlushModels();
renderer_->FlushLines(); renderer_->FlushSpheres(); renderer_->FlushSprites2D();
```

## 4. Collision — 当たり判定（`Math/Collision.h`、Topic11で軽く言及）

```cpp
Collision::Sphere s1{pos1, r1}, s2{pos2, r2};
bool hit = Collision::SphereSphere(s1, s2);

Collision::OBB obb1 = obbCollider->GetWorldOBB(obj1.transform);
Collision::OBB obb2 = obbCollider2->GetWorldOBB(obj2.transform);
bool hit2 = Collision::OBBOBB(obb1, obb2);

Collision::AABB aabb1{min1, max1}, aabb2{min2, max2};
bool hit3 = Collision::AABBAABB(aabb1, aabb2);   // Race the Sunフェーズ1で使う予定（[[project_race_the_sun]]）

// マウスピッキング用（Topic11のUpdatePickingで使用中）
float t;
bool hitRay = Collision::RaySphere(ray, sphere, t);  // tは交点までの距離（レイのスケール基準）

// フラストラムカリング用（Topic11で使用中）
Collision::Frustum frustum = Collision::MakeFrustumFromViewProjection(view * proj);
bool visible = Collision::SphereFrustum(bounds, frustum);
```
他に`AABBSphere`/`AABBSegment`/`OBBSphere`/`OBBRay`等、組み合わせごとに関数が用意されている（一覧は`Collision.h`）。

## 5. Camera（Topic9）

```cpp
camera_->HandleInput(deltaTime);                 // WASD+マウスで自由視点移動（Race the Sunでは置き換え候補）
Matrix4x4 view = camera_->GetViewMatrix();
Matrix4x4 proj = camera_->GetProjectionMatrix(camera_->GetAspectRatio(width, height));
camera_->SetPosition({0,1,-5});  camera_->SetRotation({0,0,0});  camera_->SetFov(45.0f);
const CameraData& data = camera_->GetCameraData();  // position/rotation/fov/nearClip/farClipを読む
```

## 6. Input（`Engine/InputDevice/InputDevice.h`、DIK_*定数はdinput.h、Topic9で軽く言及）

```cpp
if (Input::IsPressed(DIK_W))     { /* 押されている間ずっとtrue */ }
if (Input::IsTriggered(DIK_SPACE)) { /* 押した瞬間の1フレームだけtrue */ }
if (Input::IsMouseLeftPressed())  { ... }
if (Input::IsMouseRightPressed()) { ... }
long dx = Input::GetMouseMoveX();  long dy = Input::GetMouseMoveY();
long wheel = Input::GetMouseWheel();
```
`Input::`名前空間経由で呼ぶ（`InputDevice::GetInstance()`のシングルトンをラップしたショートカット）。`Engine::Update()`が毎フレーム`InputDevice::GetInstance().Update()`を呼んで状態を更新済みなので、Game側は読むだけでよい。

## 7. Audio（`Engine/Audio/`、Topic14）

```cpp
// Initialize時
Sound bgm;
bgm.Load("Resources/Audio/BGM.mp3");
AudioManager::GetInstance().RegisterSound("BGM", &bgm, SoundType::BGM, /*loop=*/true);

// 再生系（Soundを直接操作してもよいし、AudioManagerのDrawImGuiパネルからでも操作できる）
bgm.Play(/*loop=*/true, SoundType::BGM);
bgm.Stop();
bgm.SetVolume(0.8f);
bool playing = bgm.IsPlaying();

// マスター/バス単位の音量
AudioManager::GetInstance().SetMasterVolume(1.0f);
AudioManager::GetInstance().SetBGMVolume(0.8f);
AudioManager::GetInstance().SetSEVolume(1.0f);
```

## 8. BlendMode / TextureHandle（Topic5,6）

```cpp
enum class BlendMode { kNone, kNormal, kAdd, kSubtract, kMultiply, kScreen, kCount };
using TextureHandle = uint32_t;
static constexpr TextureHandle kTextureNone = 0;  // 白テクスチャ扱い
```

---

以上が現状Gameから触れる主要APIの全体像。Race the Sunフェーズ1（Player/AABB/Obstacle/ObstacleManager/GameStateManager）を書き始める際は、上記2(GameObject/Component)+3(Renderer)+4(Collision)+6(Input)の組み合わせが中心になる見込み。

---

## 未消化の疑問・メモ欄

（ここに、説明を聞いて気になったこと・後で調べたいことを自由に書き足す）

