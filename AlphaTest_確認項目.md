# 2値抜き(αTest) 確認項目

## 前提
テクスチャに透明部分（アルファチャンネルで抜けている部分）が含まれるものを選ぶ。
`textures_`に登録済みのものだと `monsterBall.png.png` が透過を含む可能性が高い。
無ければ、透過PNGを1枚 `Resources/` に置いて `Game::Initialize` のテクスチャ一覧に追加する。

## 確認手順

1. 対象オブジェクト（例：Cube）のImGuiパネルで
   - `Cube Texture` を透過ありのテクスチャに切り替える
   - `Cube BlendMode` は `None` のままにする（αTestはNoneと併用する想定）
2. `Cube Alpha Test` チェックボックスをONにする
3. `Cube Alpha Threshold` を 0.0 → 1.0 まで動かしてみる

## 期待する見た目

- [ ] `Alpha Test` = OFF のとき：透過部分もそのまま不透明に描画される（従来通り、変化なし）
- [ ] `Alpha Test` = ON, しきい値 = 0.0 のとき：ほぼ何も透明にならない（全ピクセルのα≥0なので通過）
- [ ] `Alpha Test` = ON, しきい値 = 0.5 のとき：テクスチャの半透明〜透明部分が「ギザギザに」完全に消える（半透明のグラデーションは出ず、0か1のどちらかになる＝2値抜き）
- [ ] `Alpha Test` = ON, しきい値 = 1.0 のとき：α=1未満の部分が全て消え、完全不透明なピクセルだけが残る
- [ ] しきい値を動かすとチェックボックスOFF中はスライダーが操作不可（グレーアウト）になっている

## 見え方のチェックポイント

- [ ] 透明になったピクセルの奥（背景やGridCube等）が透けて見える＝正しく`discard`されている証拠
- [ ] 半透明のフチが「なだらかに薄くなる」のではなく「くっきり境界線で切れる」＝Binary（2値）になっている
- [ ] 他のBlendMode（Multiply/Add等）と併用しても壊れないか（BlendModeを変えつつAlpha TestもONにして確認）

## 複数オブジェクトでの確認（任意）

同じ手順を以下でも試し、共通して同じ挙動になるか確認する。
- [ ] Sphere
- [ ] Triangle
- [ ] Model (OBJ)
- [ ] Model (FBX, Assimp)
- [ ] Sprite3D
- [ ] Sprite2D
- [ ] Grid Cubes（floorTexIndex_と違うテクスチャの場合、視認しやすいか）

## 異常時のチェック

- [ ] チェックボックスON後、画面が真っ黒/真っ白にならないか（b2の定数バッファ順序ズレの兆候）
- [ ] 他のオブジェクト（Alpha Test未使用）の見た目が変わっていないか（影響範囲が漏れていないかの確認）
