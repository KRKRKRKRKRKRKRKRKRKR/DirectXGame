# 使い方（採点者向け操作マニュアル）

`ClearRootEngine.exe`（`提出用/` フォルダ、またはルート直下のもの）の操作方法。
実行ファイルはRelease版で、起動直後はエディタパネル（Hierarchy/Inspector等）が出ない
「実際のゲーム画面のみ」の状態で始まる。

> 対象バージョン: 2026-07-24 時点のビルド。それより古い `提出用/ClearRootEngine.exe` は
> タイトル画面から一切操作できない不具合を含むため、必ず本書と同じタイミングで再ビルドされた
> exeを使うこと（詳細は本書末尾の「補足」参照）。

## 1. 起動〜遊び方（キーボードのみ）

エディタUIは出ない状態で始まるため、以下のキーだけで一通り遊べる。

| 画面 | キー | 効果 |
|---|---|---|
| タイトル | Enter | セレクト画面へ進む |
| セレクト | Enter | プレイ画面へ進む（ゲーム開始） |
| セレクト | Esc | タイトルへ戻る |
| プレイ中 | Esc | タイトルへ戻る |
| プレイ中 | F1 | ゲームオーバー画面へ強制的に進む（デバッグ用） |
| プレイ中 | （HPが0になる） | 自動的にゲームオーバー画面へ進む |
| ゲームオーバー | Enter または Esc | タイトルへ戻る |

プレイ中の実際の操作（移動等）は `Resources/Play/scene.json` に保存されたシーン構成
（PlayerController・AutoRun等のコンポーネント設定）に依存する。詳細な操作説明が画面内に
HUD/テキストとして表示される場合はそれに従うこと。

## 2. エディタ機能を見たい場合（F11）

エンジン自体には、シーンを直接編集できるエディタ機能（Hierarchy/Inspector/Projectパネル・
Gizmoでのオブジェクト移動・当たり判定の可視化・保存/読み込み等）が搭載されている。
**Debugビルドでのみ**、F11キーでこのエディタUIの表示/非表示を切り替えられる。

Release版（`提出用/ClearRootEngine.exe`）ではF11は反応しない（意図的にゲーム画面のみを
見せる仕様のため）。エディタ機能を確認したい場合は、ソースからDebug構成でビルドし直した
実行ファイルを使うこと（ビルド方法は次章）。

Debugビルドで F11 を押してエディタUIを表示すると、画面右側にHierarchy/Inspector等の
パネルが現れ、以下のような操作ができる。

- **Gizmoパネルの「再生」/「停止」ボタン**：Unityの Play/Stop に相当。停止中はオブジェクトを
  自由に配置でき、再生中のみ物理的な挙動（重力・当たり判定の押し戻し・自動前進等）が動く
- **Gizmoパネルの「Scene」/「Game」ボタン**：エディタの自由カメラ（Scene）と、シーン内に配置した
  カメラ視点（Game、実際のプレイ画面相当）を切り替えられる
- **Hierarchyパネル**：シーン内の全オブジェクトの一覧・親子構造。クリックで選択、ドラッグ&ドロップで
  親子付け替え
- **Inspectorパネル**：選択中オブジェクトの名前・タグ・保持コンポーネントの詳細編集、
  「+ コンポーネントを追加」からの機能追加
- **Projectパネル**：`Resources/` 配下の画像・音声・登録済みコンポーネント（スクリプト）一覧。
  ドラッグ&ドロップでオブジェクトへ貼り付け可能
- **「保存」/「読み込み」ボタン**：現在のシーン内容を `Resources/{シーン名}/scene.json`・`ui.json` へ
  保存/復元する
- **マウス操作（Scene表示中）**：右ドラッグで視点回転、Alt+左ドラッグで注視点を中心に周回、
  中ドラッグで平行移動、ホイールで前後移動

## 3. ソースからビルドする場合

Visual Studio 2026（`v145`ツールセット、ソリューションは `ClearRootEngine.slnx`）が必要。
CLIからビルドする場合は以下（Debug/Releaseは `/p:Configuration=` で切り替え）。

```
& "C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" `
  "ClearRootEngine.vcxproj" /p:Configuration=Debug /p:Platform=x64 /m /nologo /v:minimal
```

ビルド後の実行ファイルは `x64\Debug\ClearRootEngine.exe`（または `x64\Release\...`）に生成される。

---


制御フローの詳細は [ドキュメント.md](ドキュメント.md) を参照。
