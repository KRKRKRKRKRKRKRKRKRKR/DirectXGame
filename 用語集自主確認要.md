Heap
　GPUが使うメモリ領域の種類　
Default 読み書きできる高速領域　更新はほぼしない　読み込みメイン　Initializeで使う

Upload　CPUが書き込んでGPUが読み込む　頂点､定数バッファ転送用　毎フレーム行う　

ReadBack GPUが書き込んでCPUが読み込む　GPU計算結果をCPUに返す　

HEAPはメモリがどこに置かれるかの種別　Upload Heap だけが「橋渡し」の役割を持つ

*補足説明*
このプロジェクトの CreateBufferResource() は Upload Heap を使っている
頂点バッファや定数バッファはCPUから毎フレーム書き込みたいのでUploadを使う
テクスチャは一度ロードしたら変わらないのでDefault Heapに置かれる


Resource
GPU上のメモリ領域そのものを表すオブジェクト　何かを作るにはまず場所を確保しなくてはならない
例えば
VertexResourceは三角形の頂点座標を記憶させるための場所が必要なので作る必要があるこれは初期化のみでいい
なぜなら更新処理で形が変わるような処理は行っていないので毎フレームする必要はないもし変わるとしても一回だけ更新すればいいだけ
この処理は毎回やると重い

*補足説明*
TextureManager::CreateBufferResource() がこの確保処理を担っている
Triangle/Line の Initialize() 内で呼ばれ､頂点バッファや WVP 行列用の領域を確保している


Buffer汎用バイト列データ
頂点データ､インデックスデータ､定数などを格納するResourceの一種
頂点バッファやマテリアルバッファなど
Resourceは箱　Bufferは入れ物の種類　頂点バッファはBufferに入れる入れ物

VertexResourceは頂点バッファそのもの

*補足説明*
Triangle.cpp の Initialize() で vertexResource_ を確保し､頂点座標を書き込んでいる
Line.cpp も同じ構造で lineVertexResource_ を確保している


View　リソースののぞき窓
Resourceをどう使うかを定義する　同じResourceでもViewによって用途が変わる

*補足説明*
View の実体（ハンドル）は DescriptorHeap に並んで格納される
DescriptorHeaps クラスがこの管理を担っている


VertexBufferView　VBVの場合
頂点バッファをどう読み取るかを定義するView　バッファのどこから始まるのか､1頂点が何バイトなのか､全体が何バイトなのか
作りたいものを設定するもの　それをGPUに伝える　正四角形を作ったときもここつかった

*補足説明*
Triangle.cpp の Initialize() で vertexBufferView_ を設定し
Draw() 内で IASetVertexBuffers() に渡してGPUに「頂点はここにある」と伝えている


ShaderResourceView　SRVの場合
テクスチャや構造化バッファをシェーダーから読み取るためのView　使いたいテクスチャを設定する　

*補足説明*
TextureManager::LoadTexture() でテクスチャをロードすると
DescriptorHeaps の SRV スロットに登録され､インデックスで管理される
描画時に SetGraphicsRootDescriptorTable() でシェーダに渡している


RenderTargetView　RTVの場合　
描画結果の書き込み先を定義するView　
スワップチェーンのどのバッファに書き込むのか　このプログラムだと二個作ってるのでそこに書き込んでる毎フレーム

*補足説明*
BeginFrame() の OMSetRenderTargets() でセットされる
SwapChain のバックバッファ2枚に対して RTV を1つずつ作っているので計2個


DepthStencilView　DSVの場合
奥行きを定義するView　毎フレームオブジェクトの奥行きを自動設定してくれる優れ者

*補足説明*
BeginFrame() で OMSetRenderTargets() に RTV と一緒にセットされる
ClearDepthStencilView() で毎フレーム深度をリセットしている
これをしないと前フレームの深度情報が残って描画がおかしくなる


ConstantBufferView　CBVの場合
定数バッファ(位置､回転､スケールをまとめたもの､色など)をシェーダから参照するためのView Triangleでは色や行列､シェーダなどを渡すときにつかう　これも
毎フレーム
ここを変えることでオブジェクトの色や行列を変更することができる

*補足説明*
Triangle では wvpResource_（WVP行列）と materialResource_（色）の2つが CBV に相当する
SetGraphicsRootConstantBufferView() でシェーダの register(b0)/register(b1) に渡している


RootSignature　(シェーダへの入力規約)
Initializeのときに設定する

シェーダがどんなResourceを受け取るか契約するもの　PSOを作る前に決める
プログラムでは　マテリアルの色､WVP行列､テクスチャを入れる

*補足説明*
Pipeline.cpp の Initialize() 内で作成している
HLSL の register(b0)/register(b1)/register(t0) と対応させないとエラーになる
RootSignature を変えたら HLSL 側も合わせて変える必要がある


RootParameter
Rootsignatureの中の各スロットの定義
RootSignatureで追加したものの詳細な情報を入れる

*補足説明*
このプロジェクトでは以下の3スロットで構成されている
RootParameter[0] → CBV → register(b0) → マテリアル色
RootParameter[1] → CBV → register(b1) → WVP行列
RootParameter[2] → DescriptorTable(SRV) → register(t0) → テクスチャ


PSO　レンダリングパイプライン全体の設定をまとめたオブジェクトのこと
PSO（1つのセット）
  ├─ RootSignature     : 入力構造
  ├─ VertexShader      : 座標変換プログラム
  ├─ PixelShader       : 色計算プログラム
  ├─ BlendState        : 色合成ルール
  ├─ RasterizerState   : ポリゴン→ピクセル ルール
  ├─ DepthStencilState : 深度テスト ルール
  └─ InputLayout       : 頂点フォーマット

*補足説明*
Pipeline クラスと LinePipeline クラスがそれぞれ PSO を持っている
三角形用と線分用で別々の PSO が必要な理由はプリミティブトポロジが違うから
（三角形は TRIANGLELIST、線分は LINELIST）
描画前に SetPipelineState() で「どの PSO を使うか」を CommandList にセットする


InputLayout
頂点バッファのデータ構造を定義する　バイト列を分割してシェーダに伝える　これがないと読み込めない

*補足説明*
このプロジェクトの頂点データは position(xyz) と texcoord(uv) の2つで構成されている
InputLayout でこの2つのオフセットとバイト数を定義することで
GPU が頂点バッファのバイト列を正しく解釈できる


BlendState
ピクセルを描画するとき､すでに描かれている色を合成するか､
加算合成などを設定できるここを変えたら面白いことができそうだ
BlendState								説明					見た目
BLEND_ONE + BLEND_ZERO					新しい色で完全上書き	通常（今のプロジェクト）
BLEND_SRC_ALPHA + BLEND_INV_SRC_ALPHA	半透明合成			ガラスのように透ける
BLEND_ONE + BLEND_ONE					加算合成				光が重なる感じ
BLEND_ZERO + BLEND_SRC_COLOR			乗算合成				暗くなる

*補足説明*
TrailParticle3D のエフェクトを光らせたい場合は BLEND_ONE + BLEND_ONE（加算合成）に変えると
パーティクルが重なるほど明るく光る表現になる
Race the Sun の機体エフェクトに使えそう


RasterizerState(ポリゴンをピクセルに変換)
3DポリゴンTriangleなどを2Dのピクセルに変換する　画面に表示するときの変換ってこと
ここで塗りつぶしや2Dのとき見えないものを描画するかどうかなど設定できる

*補足説明*
カリング設定がここにある
CULL_BACK（デフォルト）: ポリゴンの裏面を描かない → 3Dオブジェクトの内側が見えない
CULL_NONE: 裏面も描く → 板ポリゴン（スプライト）で必要になることがある
FILL_WIREFRAME にするとワイヤーフレーム表示になりデバッグに使える


DepthFunc(深度比較関数)
z-Bufferの深度比較をどの方法で行うかセットする
DepthFunc						ルール				効果
LESS							新 < 既存 なら描く		標準（手前が優先）
LESS_EQUAL						新 ≤ 既存 なら描く 　	現在のプロジェクト
GREATER							新 > 既存 なら描く		奥側が優先（反転）
ALWAYS							常に描く				深度無視（透視）
NEVER							絶対に描かない	何も描かない

*補足説明*
現在 LESS_EQUAL を使っている理由: 同じ深度のオブジェクトを複数回描くとき
LESS だと2回目以降が「同じ深度 = 手前ではない」と判定されて描かれないことがある
LESS_EQUAL にすると同じ深度でも上書きできる


CommandList　毎フレームする
GPUの命令をまとめて記憶するリスト　GPUに送るものはだいたいここにセットする

*補足説明*
BeginFrame() の先頭で Reset() してリストを空にしてから命令を積んでいく
EndFrame() の ExecuteCommandLists() でまとめて GPU に送って実行させる
「メモ帳に命令を書いて、最後にまとめて渡す」イメージ


CommandQueue
CommandListをGPUに送って実行させる

*補足説明*
DirectXManager が commandQueue_ として持っている
ExecuteCommandLists() を呼ぶと CommandList の命令が GPU で実行される
実行完了を確認するためにフェンス（Fence）と組み合わせて使う


SwapChain
画面に表示するバッファを管理する仕組みのこと
描画中､描画準備のバッファを交互に切り替えて画面を更新する
これを一枚だけにするととんでもなくちらつくぞ

*補足説明*
EndFrame() の swapChain_->Present(1, 0) で切り替えが起きる
第1引数の 1 は Vsync 待ち（60fps に合わせる）、0 にすると上限なしになる
バックバッファ（描画中）とフロントバッファ（表示中）を毎フレーム入れ替えている


VertexShader
頂点座標変換を行うGPU上のプログラムシェーダのこと　3Dを2Dに変換する
シェーダのプログラム自体は変わらない　毎フレーム実行される（更新されるのはWVP行列）
これがないとオブジェクトは動かない

*補足説明*
Object3D.VS.hlsl に定義されている
WVP行列（定数バッファ）を受け取り mul() で頂点座標に掛けて変換している
行列を変えることで位置・回転・スケールが変わる → シェーダ自体は変わらない


PixelShader　
ラスタライズ後のピクセルの色を変換するテクスチャのサンプリング､ライティング､色計算など
これを変えたらライティングで遊べそう
PixelShader = 最終的な「色の見た目」を決める魔法師 ✅
遊べる効果：
ライティング（明暗）
グレースケール（白黒）
ネガ（色反転）
セピア調
ぼかし
輝度検出
エッジ検出

*補足説明*
Object3D.PS.hlsl に定義されている
テクスチャの色（gTexture.Sample）にマテリアル色を掛けて最終的な色を返している
ここを改造するだけで見た目を大きく変えられる → HLSL を触る入口として最適


HLSL
DirectXのシェーダを書くための言語のこと　C言語に近いこれをこれをマスターすれば効率化できそう
このプログラムだと
✅ 座標変換（mul で行列積）
✅ テクスチャサンプリング（Sample で色取得）
✅ 色計算（掛け算で合成）

*補足説明*
このプロジェクトでは Object3D.VS.hlsl / Object3D.PS.hlsl / SkyDome.VS.hlsl などがある
ShaderCompiler.cpp が DXC を使ってこれらをコンパイルし PSO に渡している


DXC
HLSLをGPUが実行できるようにコンパイルすること

*補足説明*
ShaderCompiler.cpp の CompileShader() 内で実行している
コンパイル済みのバイナリ（IDxcBlob）を Pipeline.cpp が受け取り PSO に設定している
HLSLを変えたらビルド時に自動でコンパイルし直される


register (HLSLResource指定)
HLSLでシェーダが受け取るResourceをどのスロットに割り当てるか指定する
RootParameterと同じにしないとエラー出るぞ

b = Buffer（定数バッファ / CBV）、t = Texture（テクスチャ / SRV）、s = Sampler の頭文字

register(b0)	RootParameter[0] CBV
register(b1)	RootParameter[1] CBV
register(t0)	RootParameter[2] SRV
register(s0)	StaticSampler

*補足説明*
b = CBV（定数バッファ）、t = SRV（テクスチャ）、s = Sampler の頭文字
HLSL側の register 番号と RootParameter のインデックスが対応していないと
シェーダが正しいデータを受け取れずに真っ黒や真っ白になる


Sampler(テクスチャ読み取り補間)
テクスチャを読み取るときの補間方法を定義する　拡縮したときなどの見た目をどうするか設定する

*補足説明*
Pipeline.cpp で StaticSampler として RootSignature に埋め込んでいる
FILTER_MIN_MAG_MIP_LINEAR（線形補間）を使っているのでテクスチャが滑らかに見える
FILTER_MIN_MAG_MIP_POINT にするとドット絵風のカクカクした見た目になる


TextureCoordinate(UV)
「3D ポリゴンのどこに、テクスチャのどこを貼るか」の指定

*補足説明*
頂点データの texcoord メンバとして格納されている
U が横方向(0.0～1.0)、V が縦方向(0.0～1.0) でテクスチャ全体を表す
UV を 0～2 にすると同じテクスチャが2回繰り返されてタイリングになる


Sampling
シェーダがテクスチャの特定のUV座標の色の読み取る処理のこと｡
UV位置周囲のTextureを補間して色を返す
Sampling = 「UV座標 → 色」に変換する処理。補間はその精度を上げる工夫。

*補足説明*
Object3D.PS.hlsl 内で gTexture.Sample(gSampler, input.texcoord) として呼ばれている
gSampler が Sampler で定義した補間方法、input.texcoord が頂点から受け取った UV 座標


Texture
データを格納するResourcesのこと

*補足説明*
TextureManager::LoadTexture() でファイルから読み込み GPU の Default Heap に転送される
TextureID というインデックスで管理され､描画時に SetGraphicsRootDescriptorTable() で渡す
一度ロードしたら毎フレーム再ロードはしない


Z-buffer　奥行きを格納するバッファのこと

*補足説明*
DSV（DepthStencilView）としてバインドされる深度専用の Resource
毎フレーム ClearDepthStencilView() でリセットする
これがないと「後から描いたオブジェクトが必ず手前に来る」状態になる


ダブルバッファリング
SwapChainがこれを実現している

*補足説明*
バックバッファ（GPU が描いている）とフロントバッファ（画面に表示中）の2枚を使う
Present() で2枚を入れ替えることで描画中の画面が画面に出ないようにする
これをしないと「描きかけの状態」が画面に映ってちらつく


Viewport
レンダリング結果をウィンドウのどの矩形領域に表示するのか設定すること

*補足説明*
BeginFrame() で RSSetViewports() に渡している
現在はウィンドウ全体に設定しているので意識しなくていいが
分割画面や PinP（ミニマップ）を作るときに複数の Viewport を使い分ける


Scissor　描画範囲切り取り
描画するピクセルを矩形範囲で切り取る範囲の設定のこと
ViewportとScissorは一つのセットで考えること

*補足説明*
BeginFrame() で RSSetScissorRects() に渡している
現在は Viewport と同じサイズ（画面全体）なので実質無効化状態
Scissor の外側のピクセルは Viewport に投影されていても描画されない
UIのはみ出し防止などに使う


Transform
オブジェクトの位置回転スケールをまとめたもの

*補足説明*
Game.cpp の triangleTransforms_[] 配列で各三角形の Transform を管理している
毎フレーム Transform から WVP 行列を計算して定数バッファに書き込んでいる


Material 
オブジェクトの色やテクスチャなどの色をせっていする
これはエンジン設計上の概念

*補足説明*
Triangle では materialResource_ として GPU バッファを持っている
color（Vector4）を書き込むと PixelShader でテクスチャ色と掛け合わせて最終的な色になる
アルファ値（w成分）を 0 にすると透明になる


Scene
ゲーム画面単位　タイトル､ゲーム中､リザルトなど

*補足説明*
現在はシーン管理クラスがないため Game.cpp にすべてまとまっている
Race the Sun 実装時に GameStateManager を作ってシーン切り替えを管理する予定


Log
バグの特定など色々知ってて当たり前

*補足説明*
このプロジェクトでは Engine/Utils/Logger.h が担っている
OutputDebugString でVisual StudioのOutputウィンドウに出力される


ImGui
言う事なし

*補足説明*
ImGuiManager クラスで BeginFrame()/EndFrame() を管理している
Game::DrawImGui() でパラメータ調整UIを実装している
実行中にパラメータをリアルタイムで変えられるのでデバッグツールとして非常に強力


PIX
Microsoftが提供するデバックツール　これがあることで楽にデバックできる
DrawCallが重いのかどうかなど､GPUの状況を確認できる

*補足説明*
GPU の各 DrawCall にかかった時間や描画順序を可視化できる
Task 2 でグリッドを 202 DrawCall → 1 DrawCall に削減した効果もここで確認できる
重い処理を特定するときは ImGui の FPS 表示と合わせて使うと原因を絞り込みやすい


シェーダ
GPU上で動く小さなプログラム
CPU､GPUとの違いは同時並行で計算できるため効率がいい
シェーダはGPUに仕事を委託するプログラム

*補足説明*
このプロジェクトでは VertexShader と PixelShader の2種類を使っている
ShaderCompiler.cpp がビルド時に HLSL ファイルをコンパイルし
Pipeline.cpp が PSO に組み込んで描画時に GPU が実行する
